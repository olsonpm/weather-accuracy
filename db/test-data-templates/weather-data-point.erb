insert into weather_data_point (weather_data_point_data_id, weather_data_point_value, weather_data_point_name_id)
values <%
dataAndLocationIDs.forEach(function(ds, i) {
    measurementNameIds.forEach(function(aMeasurementNameId) {
		var dataId = ds.dataId;
		var locationID = ds.locationID - 1;
		
        var value;
        var maxHighTemps = [16, 3, 7];
        var minHighTemps = [10,-3, 1];
        var maxLowTemps = [6,-1, 1];
        var minLowTemps = [0,-10,-5];
        var maxWindSpd = [20, 30, 20];
        var minWindSpd = [0, 10, 0];
        
        switch (+aMeasurementNameId) {
            case 1:
                value = Math.floor(Math.random() * (maxHighTemps[locationID] - minHighTemps[locationID])) + minHighTemps[locationID];
                break;
            case 2:
                value = Math.floor(Math.random() * (maxLowTemps[locationID] - minLowTemps[locationID])) + minLowTemps[locationID];
                break;
            case 3:
                value = Math.floor(Math.random() * (maxWindSpd[locationID] - minWindSpd[locationID])) + minWindSpd[locationID];
                break;
        }

		if (i < dataAndLocationIDs.length - 1 || aMeasurementNameId < 3) {
			print('(' + dataId + ', ' + value + ', ' + aMeasurementNameId + ')\n    , ');
		} else {
			print('(' + dataId + ', ' + value + ', ' + aMeasurementNameId + ');');
		}
    });
}); %>
